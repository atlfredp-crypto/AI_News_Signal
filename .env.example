# Optional. Enables the "日本語で要約" button (xAI / Grok).
# Leave empty to use the news list without summaries.
XAI_API_KEY=

# Optional override for the summarize passphrase.
# If unset, the app uses the passphrase already configured for this project.
SIGNAL_SUMMARY_PASSPHRASE=
